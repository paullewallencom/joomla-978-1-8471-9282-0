getinstance1.php

A class which demonstrates the singleton pattern in Joomla!.

getinstance2.php

A class which demonstrates how to implement getInstance() using a static array of objects.

getinstance3.php

Builds on getInstance2.php and dynamically searches a folder for subclasses.

userparameters.php

PHP snippet which adds two params tags to the users.xml file.